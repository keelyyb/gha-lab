name: test
on:
  push:
    branches: [ main ]
jobs: 
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - run: pip install pytest
      - run: pytest test/automated-test.py